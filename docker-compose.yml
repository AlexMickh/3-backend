services:
  # app:
  #   build: . 
  #   # Optional: specify an image name and tag the built image
  #   image: app:latest 
  #   ports:
  #     - "8000:8000"
  #   container_name: app
  #   networks:
  #     - app-network

  # nginx:
  #   image: nginx:alpine # Use the lightweight Alpine image
  #   ports:
  #     - "80:80" # Map host port 8080 to container port 80
  #   volumes:
  #     - ./nginx/nginx.conf:/etc/nginx/nginx.conf
  #     - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
  #   networks:
  #     - app-network
  #   depends_on:
  #     - app

  postgres:
    image: postgres:latest
    container_name: postgres_shop
    environment:
      # POSTGRES_HOST: ${DB_HOST}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      PGDATA: /data/postgres_data
    volumes:
      - postgres:/data/postgres
    ports:
      - "${DB_PORT}:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}" ]
      interval: 15s
      timeout: 10s
      retries: 5
    restart: unless-stopped

volumes:
  postgres:

networks:
  app-network:
    driver: bridge